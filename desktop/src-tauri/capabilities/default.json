{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The loading page only. The analyzer itself is served by the emi-local sidecar over http://127.0.0.1 and is given no access to Tauri APIs.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
