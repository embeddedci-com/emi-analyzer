# Local state that must never reach the build context, let alone an image.
.env
.venv/
build/
*.egg-info/
.pytest_cache/
__pycache__/
tests/
scripts/
# The openEMS image has its own build and workflow; the worker only starts FROM it.
openems-image/
